Build and install PromptLight to /Applications (macOS).

Run the install script:
```bash
./scripts/install.sh
```

Tell the user: "PromptLight installed to /Applications/Promptlight.app - launch it from Spotlight or Finder."
